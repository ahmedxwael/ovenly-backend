import { FILE_VALIDATION_CONFIG } from "@/modules/file-uploads/validators";

/**
 * File validation options (from validator config)
 */
export type FileValidationOptions = typeof FILE_VALIDATION_CONFIG;
